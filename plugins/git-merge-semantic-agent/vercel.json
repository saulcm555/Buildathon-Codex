{
  "framework": null,
  "installCommand": "npm ci && npm --prefix apps/merge-panel ci",
  "buildCommand": "VITE_MCP_URL=/api/mcp npm --prefix apps/merge-panel run build",
  "outputDirectory": "apps/merge-panel/dist"
}
